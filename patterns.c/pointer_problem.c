#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
//Complete the following function.

int nth(int n, int a, int b, int c) {
  int i;
  for(i=1;i<=n;i++)
  {
  	
  }
}

int main() {
    int n, a, b, c;
  
    scanf("%d %d %d %d", &n, &a, &b, &c);
    int ans = nth(n, a, b, c);
 
    printf("%d", ans); 
    return 0;
}
